{
  "name": "skitterspec-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "skitterspec workspaces — the base spec-driven workflow plus swappable ticketing-provider packages.",
  "license": "MIT",
  "author": "Reuben Greaves",
  "type": "commonjs",
  "packageManager": "pnpm@11.11.0",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@skitterbyte/skittership": "^1.0.0"
  },
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build-dist.js all",
    "release": "node scripts/release.js",
    "preversion": "node scripts/no-root-version.js",
    "publish:base": "node scripts/release.js skitterspec 2.0.2 --publish",
    "publish:linear": "node scripts/release.js skitterspec-linear 1.0.1 --publish",
    "publish:all": "pnpm run publish:base && pnpm run publish:linear"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/skitterbyte/skitterspec.git"
  }
}
