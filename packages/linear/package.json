{
  "name": "@skitterbyte/skitterspec-provider-linear",
  "version": "0.0.0",
  "private": true,
  "description": "Linear ticketing provider for skitterspec \u2014 the MCP adapter, config, sync skills and seam fragments that layer one-way Linear sync (repo \u2192 mirror) onto the base workflow.",
  "license": "MIT",
  "author": "Reuben Greaves",
  "type": "commonjs",
  "bin": {
    "skitterspec-linear": "bin/skitterspec-linear.js",
    "skitterspec": "bin/skitterspec-linear.js"
  },
  "files": [
    "bin",
    "src",
    "assets"
  ],
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@skitterbyte/skitterspec-common": "workspace:*",
    "@skitterbyte/skitterspec-sync-core": "workspace:*"
  },
  "engines": {
    "node": ">=18"
  }
}
